# Separate batch configuration and remove the Scheduler token requirement

- Put `批调度配置` in each Cron management row's existing three-dot dropdown, not as a standalone row button. Enable it only for saved batch-dispatch parents; a parent's own enabled state or independently paused batch state must not disable configuration.
- Move the existing priority editor and independent run-state control out of the broadcast dialog. Keep recipient selection, batch-mode activation and offset controls in broadcasting. Mount configuration controls only while the new dialog is open.
- Remove token prerequisites/headers from SWE's batch Scheduler proxies and token validation from Scheduler's shared batch-operation dependency. Preserve source/actor requirements, SWE manager/admin checks, source/parent checks, version fencing and worker limits. The follow-up request also removes Scheduler-to-SWE Cron callback authentication; other services' token handling stays unchanged.
- Security boundary: these Scheduler endpoints trust an internal caller and must be network-isolated; caller identity headers are not proof of authentication. No deployment permissions or live network settings are changed by this task.

## Follow-up: remove remaining Scheduler token dependencies

The user subsequently expanded the scope to all Scheduler-token dependencies. Remove the callback client's token constructor argument, environment constants and outgoing header, and remove token verification specifically from SWE's unified Cron callback. Keep other internal SWE/Market routes protected, preserve model authentication, source/tenant/execution identity and `claim_token` fencing. Verify callbacks with configured, missing and invalid tokens for both direct and encoded payloads; verify unrelated internal endpoints still reject unauthenticated calls. Update current Cron documentation/examples so they no longer require Scheduler tokens.

Follow-up verification: 237 targeted tests passed (all Scheduler unit tests, SWE batch proxies/control and callback tests, protected source-template routes), plus 47 internal tenant-scope/callback compatibility tests under the project WSL virtual environment. Initial tests reproduced 13 token-related failures before the fix. Final searches of Scheduler runtime, SWE Scheduler adapters/proxies and tracked deployment/configuration code find no Scheduler authentication-token reads or headers; remaining token mentions are intentional regression fixtures, unrelated service authentication or claim fencing/Cron parsing. Static unused-name/import and syntax checks passed. No live credentials or deployment configuration were modified.

## Execution

1. Update no-token HTTP/API and action/dialog tests first; verify expected failures.
2. Inspect symbol impact and implement the scoped proxy and UI changes.
3. Run relevant Python tests, Console tests, type checking and scoped formatting/lint checks. Verify normal/batch/closed-parent buttons and the configuration dialog with real components in an isolated browser fixture.
4. Update current deployment/security documentation. Preserve previous worker-history changes. No commit or package unless requested.

Engineering and CoPaw frontend skills guide this work. Superpowers tools are unavailable and repository instructions require sequential main-thread work; verification is local self-review, not independent agent review.

## Verification outcome

- Expected failures reproduced before implementation for missing-token calls and the missing configuration entry.
- 64 targeted backend tests passed, including no-token GET/PUT/initialization POST, configured/unconfigured token environments, failure/retry proxies, manager/source validation, version conflicts, and batch operations/claims.
- 33 Console tests passed across the management page, action column, priority editor and run-state control. TypeScript and scoped ESLint/Prettier checks passed. Existing jsdom/Ant Design warnings remain; a full Python Flake8 check also reports the pre-existing 81-character error string in `_ensure_batch_wakeup`, which was not changed.
- Actual components verified with isolated fixtures at 1440x900 and 1280x720: ordinary and child task buttons disabled; enabled batch parents and self-disabled batch parents configurable; separate dialog shows only priority and run-state controls; paused state can be resumed without changing the parent's own enabled state.
- Temporary preview files, browser tab and dev server cleaned up. No real database/API mutations, deployment changes, commit or package creation.

Placement correction: move the action after `查看分发用户` in the existing dropdown, retaining its disabled state and dialog handler. Restore the original 240px action column and 3010px table scroll width. Verify menu presence/order, disabled callback protection, and actual dropdown-to-dialog navigation. No backend changes, commit or package regeneration.

Placement verified: old code failed the new menu/order assertions; all 23 management-page/column tests now pass. TypeScript, scoped ESLint/Prettier and whitespace checks passed. Real component preview confirmed the row has only enable/execute/more controls, the parent menu opens the unchanged configuration dialog, and the child menu item is disabled. Temporary preview files, tab and server were cleaned up.

## Run-state placement and external timer explanation

Move the existing run-state control above the priority editor, with the divider below the control. Preserve all save handlers. Confirm the order in tests and the browser. Read-only investigation: user priorities use manually entered tags and saved metadata (no user-list request); branches use BBK_ID_MAP. Pausing writes Scheduler's independent control and gates admission/handoff, without pausing the external physical timer. Resuming repairs only batch_dispatch_external_job_id before updating the independent state; external_job_id is not the target. Do not implement external pause/resume linkage or change recipient selection as part of this explanation request.

Verified: new DOM-order assertion failed before the move; all 20 management-page/control/editor tests pass after it. Existing pause/resume, control and admission/claim suites pass 23 backend tests, with no backend edits. The real configuration dialog shows the run-state section above priority settings and its divider below it. TypeScript, scoped ESLint/Prettier and whitespace checks pass; temporary preview artifacts/server removed. External platform conclusions are source/test-based, not a live platform observation.

## Link independent pause to the external batch timer

The user now requests external pause linkage. Pause internally first under the existing operation/version checks, then stop only `batch_dispatch_external_job_id`. Never fall back to `external_job_id`; reject missing, unavailable or conflicting timer identity. If external pause fails, keep internal state paused and return an explicit partial-failure error. Repeating the existing paused-state PUT retries external synchronization; add a paused-only `同步外部暂停` button so already-paused/partially-synchronized jobs can use this safely. Refresh remains read-only. Resume ordering remains external batch timer first, internal state second. Keep running work, queued intents and all other timers unchanged; no schema changes or live external-platform mutations. Verify order, failure handling, version rejection, repeated pause, ordinary-task exclusion and UI recovery before completion.

Verified: new assertions first reproduced six backend and two frontend failures against the old code. After implementation, all 49 targeted backend tests and 20 management-page/control tests pass. RealSchedulerAdapter with mocked HTTP transport confirms the exact external endpoint, only the batch ID and runFlag=0, including business-error responses. Tests cover internal-first pause, unchanged external-first resume, state rejection without external calls, partial failure and same-state retry, and missing/no-op/conflicting timer identity. TypeScript, scoped ESLint/Prettier, Black (79 columns, py312), Flake8 and whitespace checks pass. Sequential self-review and a real-component browser fixture verify partial-failure display, read-only refresh, paused-only synchronization without transient resume, and hiding the synchronization button after resume. Existing test-environment warnings remain. Updated the current ADR, upgrade guide, Wiki and troubleshooting entry; no schema or Scheduler-service code changes for this follow-up. Temporary preview files, tab and server removed. No live external-platform operation, commit or package creation.

## Configuration layout polish

Keep the white-first Console design and existing API/event semantics. Separate the run-state heading/switch from quiet refresh/synchronization actions. Present user and primary-branch priorities in two aligned columns (one on narrow hosts), summarize selected tags instead of repeating every ID, and render bounded, keyboard-scrollable ordered lists with stable row actions. Give empty lists an explicit state. Put the primary save action in an aligned footer with new-batch effect and unsaved-state hints. Keep all user input, order changes, error recovery, run-state version fencing and pause-only external synchronization intact; no automatic save or confirmation changes. Use existing design tokens, no new dependencies or global styles.

1. Add failing tests in BatchPriorityEditor.test.tsx and BatchRunStateControl.test.tsx for ordered lists, empty states and action hierarchy.
2. Change only the two components and their local styles, plus the configuration modal's spacing/task identity in CronJobs/index.tsx and index.module.less. GitNexus reports LOW risk for all three existing component symbols; JSX usage is verified in source.
3. Run the three relevant Vitest files, TypeScript, scoped ESLint/Prettier and whitespace checks. Verify real components in an isolated browser modal at desktop and narrow widths, with long names, many selected users, empty lists, reordering, save/error and pause/refresh/sync flows. No real API calls.
4. Review changes sequentially in the main thread (repository mapping); Superpowers capabilities are unavailable. Record actual checks, remove temporary preview artifacts, and leave changes uncommitted.

Layout verification: three new assertions failed on the old layout, then all 23 tests across the priority editor, run-state control and management page passed. TypeScript, scoped ESLint/Prettier, production build and whitespace checks passed. Real-component modal screenshots verified 1280x720, 1440x900 and a narrow 560x800 host, including long task/user identifiers, bounded lists, responsive columns, explicit empty states and a sticky save footer. Browser interaction verified manual user addition/removal, reordering, preserved edits on save failure and successful retry. Run-state concurrency/error/synchronization tests remain green; their handlers were not changed. Existing dependency forwardRef, jsdom and build-chunk warnings remain. Temporary files, server and tab were removed and the viewport reset. No backend/API/schema changes, live platform mutations, commit or packaging in this layout follow-up.
